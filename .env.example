# 思源笔记API配置
# 复制此文件为 .env 并填入实际配置

# 思源笔记服务器地址 (默认: localhost)
SIYUAN_HOST=localhost

# 思源笔记端口号 (默认: 6806，如果使用HTTPS且无特殊端口可留空)
SIYUAN_PORT=6806

# 是否使用HTTPS (true/false，默认: false)
SIYUAN_USE_HTTPS=false

# API Token (在思源笔记设置→关于→API Token中获取)
SIYUAN_API_TOKEN=your_api_token_here

# HTTP Basic Auth 用户名 (如果启用了Basic Auth)
SIYUAN_BASIC_AUTH_USER=

# HTTP Basic Auth 密码 (如果启用了Basic Auth)
SIYUAN_BASIC_AUTH_PASS=

# Basic Auth 场景下是否允许将 token 放在 URL 查询参数
# 默认 false（更安全，避免 token 出现在访问日志中）
# 仅在你的网关必须使用 query token 时设为 true
SIYUAN_ALLOW_TOKEN_IN_QUERY=false

# 是否允许写入操作
SIYUAN_ENABLE_WRITE=true

# 是否启用“先读后写”围栏（默认 true）
SIYUAN_REQUIRE_READ_BEFORE_WRITE=true

# 读标记有效期（秒），过期后需重新读取（默认 3600）
SIYUAN_READ_GUARD_TTL_SECONDS=3600

# docs 命令默认返回的文档数量上限（默认 200）
SIYUAN_LIST_DOCUMENTS_LIMIT=200

# 块根ID缓存上限（默认 5000）
SIYUAN_BLOCK_ROOT_CACHE_MAX=5000
